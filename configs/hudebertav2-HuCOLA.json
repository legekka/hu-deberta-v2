{
    "name": "hudebertav2-HuCOLA",
    "model": "outputs/base-128/checkpoint-1500000",
    "tokenizer": "outputs/base-128/checkpoint-1500000",
    "output_dir": "outputs/hudebertav2-HuCOLA",
    "train_dataset": "eval_datasets/HuCOLA",
    "max_seq_length": 128,
    "batch_size": 16,
    "gradient_accumulation_steps": 1,
    "max_steps": 1000,
    "save_steps": 150,
    "num_workers": 0,
    "learning_rate": 1e-5,
    "eta_min": 1e-7,
    "warmup_steps": 25,
    "optimizer": "adamw_torch",
    "scheduler": "cosine",
    "logging_steps": 5,
    "wandb": {
        "project": "hudebertav2-HuCOLA",
        "name": "finetune-HuCOLA",
        "tags": ["finetune", "HuCOLA"]
    }
}